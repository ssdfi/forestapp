<h1>New departamento</h1>

<%= form_for [@provincia, @departamento], url: provincia_departamentos_path(@provincia, @departamento) do |f| %>
  <%= render 'form', f: f %>
<% end %>

<%= link_to 'Back', provincia_departamentos_path(@provincia) %>
