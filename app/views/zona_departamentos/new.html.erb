<h1>New zona_departamento</h1>

<%= form_for [@zona, @zona_departamento], url: zona_zona_departamentos_path(@zona, @zona_departamento) do |f| %>
  <%= render 'form', f: f %>
<% end %>

<%= link_to 'Back', zona_zona_departamentos_path(@zona) %>
